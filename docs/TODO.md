# TODO Backlog

## Near Term
- [ ] Replace placeholder SDOH code lists with authoritative PRAPARE/LOINC codes.
- [ ] Add encounter type parsing to count ED visits specifically (use Encounter.class/type).
- [ ] Add feature flag and stub UI button for creating CarePlan/ServiceRequest write-backs (behind guard).
- [ ] Integration test hitting HAPI with recorded responses (VCR/pytest) to stabilize CI.
- [ ] Streamlit Cloud deploy guide (no Docker) and automated `uv sync` step.

## Medium Term
- [ ] Add SQLite caching option for offline demos.
- [ ] Add weight tuning via YAML config and UI sliders for scoring sensitivity analysis.
- [ ] Add patient search/filter by condition and SDOH category chips.
- [ ] Accessibility pass (keyboard nav, color contrast) and usability notes.
- [ ] Architecture diagram + screen mockups for final report.
